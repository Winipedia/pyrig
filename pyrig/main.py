"""Main function template for pyrig projects.

Template copied to consuming projects by MainConfigFile. Provides an empty main()
function for customization.

Note:
    This is a template file. The actual pyrig CLI entry point is pyrig.dev.cli.cli:main.
"""


def main() -> None:
    """Main function template.

    Automatically registered as a CLI command. Customize with application logic.
    """


if __name__ == "__main__":
    main()
