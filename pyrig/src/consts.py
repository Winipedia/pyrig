"""Constants used across pyrig modules."""

STANDARD_DEV_DEPS: list[str] = ["pyrig-dev"]
"""Standard development dependencies automatically added to pyrig-based projects.

The `pyrig-dev` meta-package includes all required development tools (testing,
type checking, linting, documentation, building, etc.).
"""
