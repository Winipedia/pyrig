"""Static resource files bundled with this package.

Place static files here (templates, data files, etc.) so they are distributed
alongside the package. Access them at runtime using
``pyrig.core.resources.resource_path()`` or
``pyrig.core.resources.resource_content()``, passing this package as the
``package`` argument.
"""
