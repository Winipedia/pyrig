"""Implementation logic for the `make` family of scaffolding commands.

Each module implements one creation command: `subcommand` appends new CLI
subcommand stubs to a project's command modules, and `local` creates the
version-control-ignored config files needed for local development.
"""
