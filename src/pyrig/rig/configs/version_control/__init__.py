"""Local version control configuration management.

Manages the project's local git configuration files: `.gitignore` patterns
to prevent unwanted files from being committed, and `prek.toml` pre-commit
hook definitions that enforce code quality checks on every commit.
"""
