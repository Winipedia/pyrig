"""Local version control configuration management.

Manages the project's local git configuration files: `.gitignore` patterns
to prevent unwanted files from being committed, and `prek.toml` hook
definitions that enforce code quality checks and manage dependencies across
multiple git stages.
"""
